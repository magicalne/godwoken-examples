export const FEE = BigInt(100000); // TODO find a good fee
export const MINIMAL_CKB_CELL_CAPACITY = BigInt(6400000000 * 2); // TODO find a good capacity
export const MINIMUM_DEPOSIT_CAPACITY = BigInt(500) * BigInt(100000000); // TODO find a good deposit capacity
